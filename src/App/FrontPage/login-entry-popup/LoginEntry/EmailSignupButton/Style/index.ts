import OwlStyle from '@/webowl/OwlManipulator/OwlStyle';
import styles from './index.scss?inline';

export default class StyleEmailSignupButton extends OwlStyle {
  constructor () {
    super({ styles });
  }
}
