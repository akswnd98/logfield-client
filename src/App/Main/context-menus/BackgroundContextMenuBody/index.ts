import ContextMenuBody from '@/Popup/ContextMenu/ContextMenuBody';

export default class BackgroundContextMenuBody extends ContextMenuBody {
  constructor () {
    super({
      manipulateMenuItems: [
        
      ]
    });
  }
}
