import OwlModel from '@/owl-mo/OwlModel';
import { Network } from 'vis-network';

export type ConstructorParam = {
  container: HTMLElement;
};

// export default class BlogMap extends OwlModel<Network> {
//   protected data: Network;

//   constructor (payload: Cons) {
//     this.data = new Network({

//     })
//   }
// }